# ─────────────────────────────────────────────────────────────────────────────
# Cache-safety gate (Part B1.1)
#
# The subset of the suite that must stay green for the caching /
# synchronization work to be safe to build on. Scope:
#
#   * tenant isolation and academic-year scoping
#   * server-side authorization and role scoping
#   * notification targeting and delivery contracts
#   * mobile response contracts, stable URLs, and cache headers
#   * the B1 synchronization foundation staying inert
#   * the test environment itself failing closed
#
# Run it with:
#
#   .venv\Scripts\python.exe tests/run_cache_safety_gate.py -q
#
# tests/run_cache_safety_gate.py strips these comments and passes the rest to
# pytest (pytest's own @argfile support is argparse-based and keeps comments).
#
# This gate is NOT the whole suite. Unrelated legacy/WIP failures — PDF font
# embedding, media-proxy streaming, chat UI, fee refunds, external
# registration, school demo cleanup — are deliberately out of scope and are
# not fixed here.
# ─────────────────────────────────────────────────────────────────────────────

-p
no:cacheprovider

# ── Tenant isolation and academic-year scoping ───────────────────────────────
tests/test_tenant_isolation.py
tests/test_academic_year_guards.py
tests/test_evaluation_scoping.py
tests/test_finance_category_scoping.py
tests/test_section_delete_guards.py

# ── Authorization / role scoping ─────────────────────────────────────────────
tests/test_school_manager_access.py
tests/test_school_manager_users.py
tests/test_school_board.py
tests/test_super_admin_portal.py
tests/test_parent_requests.py
tests/test_chat_remove_member.py
tests/test_grades_student_filter.py

# ── Notifications: targeting, isolation, and unread/badge counts ─────────────
tests/test_notification_targeting.py
tests/test_live_badges.py

# ── Mobile / HTTP response contracts and cache headers ───────────────────────
tests/test_security_headers.py
tests/test_p1_stable_urls.py
tests/test_p2_caching.py
tests/test_p0_media_and_dispatch.py

# ── Synchronization foundation stays inert; test env fails closed ────────────
tests/test_sync_foundation_disabled.py
tests/test_test_environment_failclosed.py

# ── Attendance baseline (pilot resource) and cursor correctness ──────────────
tests/test_attendance_baseline.py
tests/test_sync_cursor_concurrency.py
tests/test_sync_cursor_pagination.py
tests/test_sync_scope_manifest.py
tests/test_sync_principal_state_cleanup.py

# ─────────────────────────────────────────────────────────────────────────────
# KNOWN BLOCKERS — pre-existing failures inside the relevant areas.
#
# Deselected so the gate is usable as a regression detector, NOT because they
# are dismissed. None is caused by B1/B1.1 — all fail identically on HEAD
# afbad5fe.
#
# The two B2-BLOCKING entries that used to live here are RESOLVED (pre-B2 pass):
#   * tenant_isolation::test_current_year_default_and_historical_opt_in was
#     replaced by test_students_persist_across_years_but_never_across_schools,
#     which pins the documented rule (Student is a master record that persists
#     across years) plus the school boundary that must hold in both years.
#   * live_badges::test_admin_counts_scoped_to_own_school now plants the
#     hostile cross-school row via skip_tenant_validation and proves school A
#     sees neither the count nor any school B identifier. scoping.py:621 was
#     NOT weakened - test_cross_school_complaint_is_rejected_by_the_write_guard
#     pins that the normal write path still refuses the row.
#
# What remains below is template drift against uncommitted UI work only.
# ─────────────────────────────────────────────────────────────────────────────

# [3] Template drift, not isolation: the render-capture hook never fires, so
#     the assertion reads a context key that was never captured
#     (KeyError: 'evals'). The route's own scoping is unverified by this test
#     while it is red.
--deselect
tests/test_evaluation_scoping.py::EvaluationScopingTest::test_normal_user_index_does_not_show_other_school_evaluations

# [4] Template drift: the page renders 200 but no longer contains
#     data-filter-target="subjectOptions". Server-side scoping in this file is
#     still covered by its passing sibling tests.
--deselect
tests/test_school_manager_users.py::SchoolManagerUsersTest::test_manager_create_form_scopes_searchable_student_and_subject_options

# [5][6] Template drift: KeyError 'results_view' — the capture hook keys on a
#     template context variable the current template no longer sets.
--deselect
tests/test_grades_student_filter.py::GradeStudentFilterTest::test_student_filter_does_not_affect_default_exam_list
--deselect
tests/test_grades_student_filter.py::GradeStudentFilterTest::test_student_name_filter_limits_subject_report
